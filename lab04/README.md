# Документация для четвертой лабораторной работы по ООАП

# Тема лабораторной: Архитектурные паттерны

---

Предметная область: Библиотека

1. Проблема: При работе с базой данных в системе библиотеки возникала ситуация, когда одни и те же объекты — например, данные о читателе или книге — запрашивались многократно в рамках одной операции.
   Каждый такой запрос выполнялся напрямую к базе данных, даже если эта информация уже была получена ранее. Это приводило к избыточной нагрузке на базу данных,  увеличению времени отклика и неэффективному использованию ресурсов.
2. Решение: Для устранения этой проблемы был применён паттерн проектирования `Коллекция объектов`. Его суть заключается в том, что в памяти приложения создаётся специальная коллекция, в которой хранятся уже загруженные из базы объекты.
При поступлении нового запроса система сначала проверяет эту коллекцию. Если нужный объект уже там есть — он возвращается из памяти, без обращения к базе данных. Если объекта нет — выполняется запрос к базе, после чего результат сохраняется в коллекции для будущего использования.

3. Диаграмма классов

<img width="1119" height="627" alt="image" src="https://github.com/user-attachments/assets/6c40163c-acaf-433a-b3fe-a008bf69757d" />

4. Благодаря этому подходу каждый объект загружается из базы данных только один раз за сессию. Все последующие обращения к тому же объекту обслуживаются мгновенно из памяти. Это значительно снижает количество запросов к базе данных, уменьшает нагрузку на неё и ускоряет работу приложения, особенно при частых повторных обращениях к одним и тем же данным. Паттерн также обеспечивает целостность данных: в рамках одной сессии все части программы работают с одним и тем же экземпляром объекта, что исключает рассинхронизацию.
